				TODO
				====

15-Mar-2000

- check pager  stuff formats
- check M$ MAPI
- thinker about wall mesages
- abstract a *net.c fileall networking code ?
- debug levels
- redo line format while keeping "<" and ">" usage (prevent new syslog proto)
- minimize global variables even more
- CHECK LIBC and GLIBC's way to work
- program execution module


20-Mar-2000

1- finish modularization and classic module
2- change initialization, write modules om_init()
3- create 'program' module
4- redo parsing of config using isspace
5- change static linking to some configurable dynamic module handling

21-Mar-2000

1- take out unused includes on syslogd.c

23-Mar-2000

1- think some way to specify multiple modules on syslog.conf

13-Apr-2000

1- Check configuration parsing, there is at least 1 buffer overflow

17-Apr-2000

1- remove or at least integrate consfile using and creation

FOR FUTURE WORKING

For handling errors logging in a particular module, create a om_onerror
function to do proper reinit or whatever (and after that the module should
get in the exactly same state as before the error).

Check for buffer overflows, as we now may get things from the outside world.

11-May-2000

1- We need to change the decode function on syslogd.c and use the strcasecmp()
function

23-May-2000

1- Modules initialization should be dinamic
2- Kill filed structure
3- Optimize each input/output module
4- Create goods Makefiles
5- Complete and fix man pages
6- Udp input module from an arbitrary port
7- Linux input module to eliminate klogd on that system
8- Allow not only one module type on each configuration line
9- Add linux extensions on selector field on configuration file
10- Add support to log on linux pseudo terminals (pts/x) (UPDATE: not really good)

29-May-2000

1- We can't do select when im->path = NULL and im->fd >= 0 but the module
   should be called.
   We need to allow several others log methods like syscalls (ex.: im_linux)
   Problem: klogctl syscall con linux blocks until the kernel logs something,
   a signal should be used to stop this block


31-May-2000

1- A script to generate a static_modules.h and set an array of structs there for
   each static module.

01-Jun-2000

1- Improve om_filter so it can rewrite messages. maybe resend to input. im_filter?

