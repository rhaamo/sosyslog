/*	$Id: NEWS,v 1.15 2002/09/27 22:37:05 alejo Exp $	*/

1.09
 * im_file module updated, introduces non-poll file
 * im_serial module introduced
 * im_directory module introduced
 * im_regex handles substitution

1.03
 * initial port to AIX
 * tcp modules
 * debug levels
 * reconnect on mysql/pgsql modules

1.02
 * clean up for Linux

1.01 Release Notes:

 * solaris port
 * major autoconf cleanup
 * multiple SQL module support
 * many bugfixes

1.0 Release Notes:

* Move to autoconf 
* Reorganize the source tree
* Fix an incorrect free that was causing syslogd to coredump on exit
* New om_regex module to filter and redirect depending on contents
* New im_linux for handling linux kernel logging (like klogd)
* PostgreSQL output module, colaboration from Oliver Teuber
  (ot@penguin-power.de)
